platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven:3-jdk-8

inputs:
  - name: core-repository

outputs:
  - name: core-built

run:
  path: sh
  args:
  - -exc
  - |
    mvn clean install